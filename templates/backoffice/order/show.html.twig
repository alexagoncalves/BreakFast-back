{% extends 'base.html.twig' %}

{% block title %}Commande nº {{ order.id }}{% endblock %}

{% block body %}
    <div class="container">
        <h1>Détail Commande nº {{ order.id }}</h1>

        {{ include('backoffice/order/_delete_form.html.twig') }}
        
        <a class="btn btn-outline-light text-uppercase mb-2" href="{{ path('app_backoffice_order_index') }}">retour à la liste</a>

        <a class="btn btn-outline-light text-uppercase mb-2" href="{{ path('app_backoffice_order_edit', {'id': order.id}) }}">mettre à jour</a>

        <table class="table table-dark table-striped">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ order.id }}</td>
                </tr>
                <tr>
                    <th>Prix Total</th>
                    <td>{{ order.totalPrice }}</td>
                </tr>
                <tr>
                    <th>Date de Commande</th>
                    <td>{{ order.orderDate ? order.orderDate|date('Y-m-d') : '' }}</td>
                </tr>
                <tr>
                    <th>Utilisateur</th>
                    <td>{{ order.user.name }}</td>
                </tr>
            </tbody>
        </table>
    </div>
{% endblock %}
