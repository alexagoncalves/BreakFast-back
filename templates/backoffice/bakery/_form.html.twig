{{ form_start(form) }}
    <div class="container">
        {{ form_widget(form, {'attr': {'class': 'text-uppercase'}}) }}
    </div>
    <button class="btn btn-outline-light text-uppercase mt-2">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
